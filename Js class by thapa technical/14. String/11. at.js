let str = 'ABCDEFabcdef';


console.log(str.at('d')); // int value needed mean we give index on side parameter. if we not give index, then it return the value of first index [0]  

console.log(str.at(2)); 

//same like charAt 
            // but allows (-index)
console.log(str.at(-2)); 
